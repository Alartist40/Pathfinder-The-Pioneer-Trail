import React from 'react';
import ReactDOM from 'react-dom/client';
import '@fontsource/press-start-2p'; // Imports the "Press Start 2P" font
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
